@model IEnumerable<string>

@Html.ActionLink("Home", "List", new { controller = "Product", category = (string)null, page =1 })

@foreach (var link in Model)
{
    @Html.RouteLink(link, new
{
    controller = "Product",
    action = "List",
    category = link,
    page = 1
},
new { @class= link == ViewBag.SelectedCategory? "selected": null}
) 
}
<h2></h2>